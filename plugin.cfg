# Plugins used by the OpenShift cluster-dns-operator Corefile template.
# This is the source of truth for the OpenShift plugin set.
#
# Run `make -f Makefile.ocp generate-plugins` after editing this file.
#
# Format: <plugin-name>:<package-name>
# Internal plugins use short names (resolved to plugin/<name>).
# External plugins use fully-qualified Go import paths.

cancel:cancel
reload:reload
bufsize:bufsize
ready:ready
health:health
prometheus:metrics
errors:errors
log:log
chaos:chaos
ocp_dnsnameresolver:github.com/openshift/coredns-ocp-dnsnameresolver
cache:cache
kubernetes:kubernetes
loop:loop
forward:forward
